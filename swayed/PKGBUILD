# Maintainer: Vianney Bouchaud <aur dot vianney at bouchaud dot org>

pkgname=swayed
pkgver=1
pkgrel=7
pkgdesc="Dependency management for my own sway configuration."
url="https://bouchaud.org"
arch=('any')
license=('GPL')
depends=(
    # sway related projects
    sway swaylock swayidle swaybg
    # missing deendency for sway scripts
    python-i3ipc

    # like sway, but more awesome
    swaysome

    # screen capture
    grim slurp

    # notification service
    mako

    # default app
    ## terminal
    foot
    ## browser
    firefox
    ## mail
    thunderbird
    ## menu
    dmenu
    ## editor
    doomed

    # screen sharing for browsers
    xdg-desktop-portal-wlr

    # screen brightness
    brightnessctl

    # player controler
    playerctl

    # meh
    xorg-xwayland
)
